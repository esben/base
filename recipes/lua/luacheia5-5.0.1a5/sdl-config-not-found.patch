diff -urN luacheia5-5.0.1a5~orig/acinclude.m4 luacheia5-5.0.1a5/acinclude.m4
--- luacheia5-5.0.1a5~orig/acinclude.m4	2003-11-28 18:25:54.000000000 +0100
+++ luacheia5-5.0.1a5/acinclude.m4	2012-10-31 11:19:02.231472546 +0100
@@ -40,6 +40,8 @@
   no_sdl=""
   if test "$SDL_CONFIG" = "no" ; then
     no_sdl=yes
+  elif ! which "$SDL_CONFIG" ; then
+    no_sdl=yes
   else
     SDL_CFLAGS=`$SDL_CONFIG $sdlconf_args --cflags`
     SDL_LIBS=`$SDL_CONFIG $sdlconf_args --libs`
diff -urN luacheia5-5.0.1a5~orig/aclocal.m4 luacheia5-5.0.1a5/aclocal.m4
--- luacheia5-5.0.1a5~orig/aclocal.m4	2004-03-04 17:33:43.000000000 +0100
+++ luacheia5-5.0.1a5/aclocal.m4	2012-10-31 11:25:21.831463033 +0100
@@ -45,14 +45,18 @@
      fi
   fi
 
+  echo "before SDL_CONFIG=$SDL_CONFIG"
   AC_REQUIRE([AC_CANONICAL_TARGET])
   PATH="$prefix/bin:$prefix/usr/bin:$PATH"
   AC_PATH_PROG(SDL_CONFIG, sdl-config, no, [$PATH])
+  echo "after  SDL_CONFIG=$SDL_CONFIG"
   min_sdl_version=ifelse([$1], ,0.11.0,$1)
   AC_MSG_CHECKING(for SDL - version >= $min_sdl_version)
   no_sdl=""
   if test "$SDL_CONFIG" = "no" ; then
     no_sdl=yes
+  elif ! which "$SDL_CONFIG" ; then
+    no_sdl=yes
   else
     SDL_CFLAGS=`$SDL_CONFIG $sdlconf_args --cflags`
     SDL_LIBS=`$SDL_CONFIG $sdlconf_args --libs`
diff -urN luacheia5-5.0.1a5~orig/configure luacheia5-5.0.1a5/configure
--- luacheia5-5.0.1a5~orig/configure	2004-03-04 17:33:46.000000000 +0100
+++ luacheia5-5.0.1a5/configure	2012-10-31 11:25:12.999463257 +0100
@@ -22137,6 +22137,8 @@
   no_sdl=""
   if test "$SDL_CONFIG" = "no" ; then
     no_sdl=yes
+  elif ! which "$SDL_CONFIG" ; then
+    no_sdl=yes
   else
     SDL_CFLAGS=`$SDL_CONFIG $sdlconf_args --cflags`
     SDL_LIBS=`$SDL_CONFIG $sdlconf_args --libs`
